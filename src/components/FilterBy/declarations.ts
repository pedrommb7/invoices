export interface InvoiceFilters {
    orderBy: string;
    invoiceState: string;
    client: string;
    dateFrom: string;
    dateTo: string;
}

export interface FilterByProperties {
    /* isFilterShown: boolean; */
}